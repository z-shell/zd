---
name: Run
on:
  workflow_dispatch:


jobs:
  triage:
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
    container:
      image: ghcr.io/z-shell/zd:latest
    steps:
         - name: Run
           working-directory: .
           run: |
             zi update
             
